/**
 * Simple function to help substract two values between them
 * @param {number} first First number to substract
 * @param {number} second Second number substract to the first
 * @returns  The second number subtracted from the first
 * @exemple
 * substract(10 - 100)
 * // => 90
 */
export declare const substract: (first: number, second: number) => number;
